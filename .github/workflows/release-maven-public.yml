name: Release Maven Public

on: workflow_dispatch

jobs:
  release:
    uses: octopusden/octopus-base/.github/workflows/common-java-maven-release.yml@v3.0.0
    with:
      flow-type: public
      java-version: '21'
    secrets: inherit
