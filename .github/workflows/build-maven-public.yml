name: Build Maven Public

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: octopusden/octopus-base/.github/workflows/common-java-maven-build.yml@939bdf9b607a5cbcc80c1bed41dd44528b051ee4
    with:
      java-version: '11'
