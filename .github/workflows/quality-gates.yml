name: Quality Gates

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@e6a3edb676b6fcffedfde921ffd52eb892172be4
    with:
      java-version: '21'
