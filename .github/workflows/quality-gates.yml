name: Quality Gates

on:
  workflow_call:
  workflow_dispatch:

jobs:
  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@0403c2a3aba8a37e10a36c6a04b0571843f30ea5
    with:
      java-version: '21'
