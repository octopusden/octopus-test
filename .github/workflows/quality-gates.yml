name: Quality Gates

on:
  workflow_call:
  workflow_dispatch:

jobs:
  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@d50d0a0891a5456622c5c0f6f5152da8a50635f5
    with:
      java-version: '21'
