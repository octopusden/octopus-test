name: Quality Gates

on:
  workflow_call:
  workflow_dispatch:

jobs:
  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@9dcf446a0cd16c238d8ab0e66db40df7bcbd424d
    with:
      java-version: '21'
