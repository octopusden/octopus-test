name: Security Reports

on:
  workflow_call:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@d50d0a0891a5456622c5c0f6f5152da8a50635f5
    with:
      java-version: '21'
      enable-dependency-check: false
