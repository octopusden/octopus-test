name: Security Reports

on:
  workflow_call:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@b41289a460bab00cb467b768eb84c138ce7f7cd4
    with:
      java-version: '21'
      enable-dependency-check: false
