name: Security Reports

on:
  workflow_call:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@806882b116b621f6e3e56682e0988434ffd5b7a3
    with:
      java-version: '21'
      enable-dependency-check: false
