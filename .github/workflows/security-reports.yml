name: Security Reports

on:
  workflow_call:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@0ff816057055fc3dc6bb0116c0142df18dfe5d5a
    with:
      java-version: '21'
      enable-dependency-check: false
