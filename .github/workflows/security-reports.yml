name: Security Reports

on:
  workflow_call:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@ff80643474de6513cd1c1d538c41438c3d5cf177
    with:
      java-version: '21'
      enable-dependency-check: false
