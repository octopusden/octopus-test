name: Test check artifact and register release

on: workflow_dispatch

permissions:
  contents: read

jobs:
  build:
    uses: octopusden/octopus-base/.github/workflows/common-check-and-register-release.yml@a6b15bbea5859234372d50bc95334005a7a7c187
    with:
      artifact-pattern: "octopus-test/_VER_/octopus-test-_VER_.jar"
    secrets: inherit
